# Turn the engines range in package.json into a hard failure at install time.
# Without this npm only warns, and a too-old Node surfaces much later as an
# opaque Vite or Astro parse error instead of "your Node is too old".
engine-strict=true
